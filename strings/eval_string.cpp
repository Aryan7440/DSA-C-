#include <bits/stdc++.h>
using namespace std;

int eval(string s)
{
    stack<int> s;
    // int prev=s[0],res=0,curr=0;
    s.push_back(s[0]);
    int j = 1;
    int res=0;
    while (j < s.size())
    {
        if(s[i]=='+')
        {
            res=res+

        }
        else
        {
        }
    }
}