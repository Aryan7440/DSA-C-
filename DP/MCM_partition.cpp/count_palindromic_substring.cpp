#include<bits/stdc++.h>
using namespace std;
int util(string s,int i,int j)
{
    if(i==j)return 1;
    if()
    // for (int k = 0; k <= j; k++)
    // {
    //     int l=util(s,i,k);
    //     int r=util(s,k+1,j);

    // }
    
}